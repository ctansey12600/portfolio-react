// import React from "react";
// import Carousel from "react-bootstrap/Carousel";

// function ImgCarousel({ photoData }) {
//   const photoDataCollection = photoData.map((photolink) => (
//     <Carousel.Item key={photolink}>
//       <img className="d-block w-100" src={photolink} alt={photolink} />
//     </Carousel.Item>
//   ));

//   return <Carousel>{photoDataCollection}</Carousel>;
// }

// export default ImgCarousel;
